# Leave unset in local dev to use the Vite dev server proxy to the API (same origin cookies).
# Set when the API is hosted on a different origin than the SPA build.
# VITE_API_URL="https://api.example.com"
